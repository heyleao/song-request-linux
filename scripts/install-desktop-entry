#!/usr/bin/env bash
set -euo pipefail

APP_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
DESKTOP_DIR="${XDG_DATA_HOME:-$HOME/.local/share}/applications"
DESKTOP_FILE="$DESKTOP_DIR/song-request-linux.desktop"

mkdir -p "$DESKTOP_DIR"
chmod +x "$APP_DIR/scripts/song-request-linux-open" "$APP_DIR/scripts/song-request-linux-stop"

cat >"$DESKTOP_FILE" <<EOF
[Desktop Entry]
Type=Application
Name=Song Request Linux
Comment=Twitch song requests for Spotify and YouTube
Exec=$APP_DIR/scripts/song-request-linux-open
Terminal=false
Categories=AudioVideo;Audio;Network;
StartupNotify=true
EOF

update-desktop-database "$DESKTOP_DIR" >/dev/null 2>&1 || true
echo "$DESKTOP_FILE"
